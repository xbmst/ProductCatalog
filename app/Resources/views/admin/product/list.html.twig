{% extends 'base.html.twig' %}

{% block body %}
    {% include 'breadcrumbs.html.twig' %}
    <div class="container">
        <div class="row">
            <div class="col s12">
                <div class="row valign-wrapper">
                    <div class="col s9 valign"><h2>Products</h2></div>
                    <div class="col s2 valign">
<<<<<<< HEAD
                        {% if is_granted("ROLE_MANAGE_PRODUCT") %}
                            <a href="{{ path('admin_product_new') }}" class="btn">
                                Add<i class="material-icons right">add_circle_outline</i>
                            </a>
                        {% endif %}
=======
                        <a href="{{ path('admin_product_new') }}" class="btn left"><i class="material-icons left">add_circle_outline</i>product</a>
                    </div>
                    <div class="col s2 valign">
                        <a href="{{ path('admin_category_new') }}" class="btn"><i class="material-icons left">add_circle_outline</i>category</a>
>>>>>>> ac92def0aa2d7e24bb0246ff8768634cd70a9451
                    </div>
                </div>
                <div class="col s12">

                    <table class="striped bordered">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th data-field="product">Product</th>
                                <th>Category</th>
                                <th>SKU</th>
                                <th>Description</th>
                                <th>Created at</th>
                                <th>Updated at</th>
                                {% if is_granted("ROLE_MANAGE_PRODUCT") %}
                                    <th> </th>
                                    <th> </th>
                                {% endif %}
                            </tr>
                        </thead>
                        <tbody>
<<<<<<< HEAD
                        {% if products %}
                            {% for product in products %}
                                <tr>
                                    <td>{{ product.id }}</td>
                                    <td>{{ product.name }}</td>
                                    <td>{{ product.category }}</td>
                                    <td>{{ product.sku }}</td>
                                    <td>{{ product.description }}</td>
                                    <td>{{ product.createdAt|date("d/m/Y") }}</td>
                                    <td>{{ product.updatedAt|date("d/m/Y") }}</td>
                                    {% if is_granted("ROLE_MANAGE_PRODUCT") %}
                                    <td>
                                        <a href="{{ path('admin_product_edit', {'id': product.id}) }}"><i class="material-icons grey-text">edit</i></a>
                                    </td>
                                    {% endif %}
                                </tr>
                            {% endfor %}
                        {% endif %}
=======
                        {% for product in products %}
                            <tr>
                                <td>{{ product.id }}</td>
                                <td>{{ product.name }}</td>
                                <td>{{ product.category }}</td>
                                <td>{{ product.sku }}</td>
                                <td>{{ product.description }}</td>
                                <td>{{ product.createdAt|date("d/m/Y") }}</td>
                                <td>{{ product.updatedAt|date("d/m/Y") }}</td>
                                {% if is_granted("ROLE_MANAGE_PRODUCT") %}
                                    <td>
                                        <a href="{{ path('admin_product_edit', {'id': product.id}) }}"><i class="material-icons grey-text">edit</i></a>
                                    </td>
                                    <td>
                                        <a href="{{ path('admin_product_delete', {'id': product.id}) }}"><i class="material-icons grey-text">delete</i></a>
                                    </td>
                                {% endif %}
                            </tr>
                        {% endfor %}
>>>>>>> 0c1086cb0e7a405ea5af606416302da92ff51a23
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock body%}

{% if errors %}
    {% block javascripts %}
        {{ parent() }}
        {% include 'form-error.html.twig' with {'error': errors} %}
    {% endblock %}
{% endif %}
