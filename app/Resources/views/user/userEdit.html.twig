{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s6">
                <h2>Edit user</h2>

                {{ form_start() }}
                    {{ form_row(form.) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}